基本逻辑是：
1、注册3个传感器，加速度计、陀螺仪、game_rotation；
2、采样线程：200/s采样、10帧滑动窗口，采集到的新数据通过game_rotation将加速度计、陀螺仪统一至初始坐标系（点“开始”的那一时刻？）；
3、模型计算协程：每滑10帧扔200帧到网络模型中，网络输出结果为预测速度：Vx, Vy；
4、拿到速度后，再计算时间差，坐标增量 = 速度 * 时间差；
5、更新currentLoc，将currentLoc更新到UI。